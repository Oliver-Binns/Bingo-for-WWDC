# Bingo-for-WWDC
###Ready for WWDC16?

A basic bingo app which reads in a list of strings from JSON and displays it on screen for the user.
Each item can be marked off, by tapping on it, as the user hears it called out.
The app also utilises the iOS Sharesheet allowing bingo results to be shared via Twitter and others
